namespace CmdExecuter.Core.Models {
    public enum MainMenuSelection {
        ScanFolder = 0,
        DisplayInfo = 1
    }

    public enum ExportOptions {
        Export = 0,
        Dismiss = 1
    }
}
